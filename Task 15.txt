Task15: Task on Logical operator in mySql
employee(emp_id INT PRIMARY KEY,name VARCHAR(50),department VARCHAR(30),salary INT,ecage INT,gender VARCHAR(10),city VARCHAR(30));
1.Write a query to display employees who work in the 'IT' department AND have a salary greater than 50,000.
2.Write a query to display employees who are from 'Mumbai' OR 'Delhi'.
3.Write a query to display employees who are NOT from the 'HR' department.  
4.Write a query to display employees who are from 'Finance' XOR have salary greater than 40,000.
5.Write a query to display employees who are in the 'Sales' department AND (salary > 30,000 OR age < 25).
6.Write a query to display employees who are NOT from 'Pune' AND have salary greater than 35,000.
7.Write a query to display employees who are from 'IT' OR NOT from 'Finance'.
8.Write a query to display employees who are male XOR have salary above 60,000 AND belong to 'HR'.
9.Write a query to display employees who are age > 30 AND (department = 'IT' OR department = 'Finance').
10.Write a query to display employees who are NOT from 'Delhi' XOR have salary less than 25,000.

use chaitalijadhavdb;
create table employee(
emp_id int primary key,
name varchar(50),
department varchar(30),
salary int,
age int,
gender varchar(10),
city varchar(30)
);

insert into employee (emp_id,name,department,salary,gender,city)values
(1,'amit sharma','it',60000,'male','mumbai'),
(2,'neha verma','hr',45000,'female','delhi'),
(3,'rahul gupta','fiance',38000,'male','pune'),
(4,'sakshi mehta','sales',32000,'female','banglore'),
(5,'pratham joshi','it',52000,'male','delhi');

select*
from employee
where department='it' and salary>50000;


select*
from employee
where city='mumbai' or 'delhi';


select*
from employee
where department  <> 'hr';

select*
from employee
where (department='fiance') xor( salary>40000);

select*
from employee
where department='sales' and( salary>30000 or age<25);

select*
from employee
where city <>'pune' and salary>35000;   -- not equal to

select*
from employee
where department='it' or department <> 'fiance';

select*
from employee
where( gender='male') xor( salary>60000 and  department='hr');

select*
from employee
where age=30 and( department='it'or department='fiance');

select*
from employee
where (city<>'delhi') xor( salary<25000);